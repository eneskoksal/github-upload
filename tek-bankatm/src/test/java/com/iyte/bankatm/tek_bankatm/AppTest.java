package com.iyte.bankatm.tek_bankatm;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

import java.time.LocalDate;

import javax.money.MonetaryAmount;

import org.javamoney.moneta.Money;
import org.junit.Test;


public class AppTest 
{
	/*
	Bank TestBank = new Bank();
    ATM TestATM = TestBank.getMyATM();
    OperatorPanel TestOP = TestATM.getMyOperatorPanel();
    MonetaryAmount maxWithdrawPerDayAccount = Money.of(4000, "USD");
    MonetaryAmount CashOnHand = Money.of(158743, "USD");
	int minWithdrawPerTransaction = 20;
	int maxWithdrawPerTransaction = 20000;
	Account MainAccount = TestBank.createNewAccount(1234, "123", 0, 33333, LocalDate.of(2022, 1, 8));
	Card TestCard = MainAccount.getMyCard();
	
	@Test
    public void TheATMTests(){
        TestOP.initializeATM(CashOnHand, minWithdrawPerTransaction, maxWithdrawPerTransaction, maxWithdrawPerDayAccount);
        assertEquals(CashOnHand,TestATM.getCashOnHand());
        assertEquals(minWithdrawPerTransaction, TestATM.getMinWithdrawPerTransaction());
        assertEquals(maxWithdrawPerTransaction, TestATM.getMaxWithdrawPerTransaction());
        assertEquals(maxWithdrawPerDayAccount, TestATM.getMaxWithdrawPerDayAccount());
        assertEquals(TestBank, TestATM.getMyBank());
        
        //TestATM.userInsertedCard(TestCard); // Enter correct password 

        
    }
	
	@Test
    public void TheAccountTests(){
		
    }
	*/
}

